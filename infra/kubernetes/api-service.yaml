apiVersion: v1
kind: Service
metadata:
  name: teamtasks-api-svc
  labels:
    app: teamtasks-api
    component: api
spec:
  selector:
    app: teamtasks-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
